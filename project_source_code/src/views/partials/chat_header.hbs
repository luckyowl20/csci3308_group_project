<!-- views/partials/chat_header.hbs -->
<div class="p-3 border bg-white mb-3 rounded">
  {{#if user}}
    <h5 class="mb-0 alt-text font-alt fw-bold">Chat with: {{user.username}}</h5>
  {{else}}
    <h5 class="mb-0 alt-text font-alt fw-bold">Select a friend to chat</h5>
  {{/if}}
</div>
