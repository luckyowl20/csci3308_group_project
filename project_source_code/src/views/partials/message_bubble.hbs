{{#if is_sender}}
  <div class="text-end mb-2">
    <span class="badge bg-primary">{{message_text}}</span>
  </div>
{{else}}
  <div class="text-start mb-2">
    <span class="badge bg-secondary">{{message_text}}</span>
  </div>
{{/if}}
