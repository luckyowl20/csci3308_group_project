{{!-- <!-- views/pages/swipe.hbs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lucky Moment Swiping Page</title>
    <link rel="stylesheet" href="/resources/css/swipe.css">
    <!-- Add Bootstrap for carousel -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
    {{#if noUsersLeft}}
        <div class="no-users">
            <h2>No more users to swipe!</h2>
            <p>Check back later for new users.</p>
        </div>
    {{else}}
        <div class="swipe-container">
            {{> swipe_users user=user}}  
            <button id="left-swipe" class="swipe-button no-button">
                <i class="bi bi-x-lg" style="font-size: 2rem;"></i>
            </button>
            <button id="right-swipe" class="swipe-button yes-button">
                <i class="bi bi-heart-fill" style="font-size: 2rem;"></i>
            </button>
        </div>
        <div id="match-modal" class="modal">
            <div class="modal-content">
                <h2>It's a match!</h2>
                <p>You and {{user.display_name}} have liked each other.</p>
                <button id="close-match-modal">Continue Swiping</button>
            </div>
        </div>
    {{/if}}
    <!-- Add Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/resources/js/swipe_script.js"></script>
</body>
</html> --}}

{{!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Swipe Music</title>
  <link rel="stylesheet" href="/resources/css/swipe.css">
</head>
<body>
  {{#if noTracksLeft}}
    <h2>No tracks found</h2>
  {{else}}
    <div class="swipe-container">
      <div class="track-card">
        {{!-- <h2>{{track.name}}</h2>
        <p>by {{track.artists.[0].name}}</p>
        <img src="{{track.album.images.[0].url}}" alt="Album cover" style="width:200px;">
        <audio controls src="{{track.preview_url}}"></audio> --}}
        {{!-- <iframe style="border-radius:12px" src="https://open.spotify.com/embed/track/{{profile.spotify_song_id}}"
        width="100%" height="80" frameBorder="0"
        allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy">
      </iframe>
      </div>

      <button id="left-swipe" class="swipe-button no-button">Skip</button>
      <button id="right-swipe" class="swipe-button yes-button">Like</button>
    </div> --}}


    {{!-- Swipe Buttons --}}
    {{!-- <div class="d-flex justify-content-center gap-3">
      <button id="left-swipe" class="btn btn-outline-danger">Skip</button>
      <button id="right-swipe" class="btn btn-outline-success">Like</button>
    </div> --}}

  {{!-- </div>
</div>

  {{/if}}

  <script>
  </script>
</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Explore Music</title>
  <link rel="stylesheet" href="/resources/css/swipe.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div class="swipe-container">
    <div class="track-card">
      <h2>Random Music Explorer</h2>
      <iframe 
        style="border-radius:12px" 
        src="https://open.spotify.com/embed/track/{{trackId}}"
        width="100%" 
        height="352" 
        frameBorder="0" 
        allowfullscreen="" 
        allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" 
        loading="lazy">
      </iframe>
    </div>

    <div class="swipe-buttons">
      <button id="left-swipe" class="swipe-button no-button">Skip</button>
      <button id="right-swipe" class="swipe-button yes-button">Like</button>
    </div>
  </div>

  <script>
    document.getElementById('left-swipe').addEventListener('click', () => {
      window.location.reload();
    });

    document.getElementById('right-swipe').addEventListener('click', () => {
      alert("Liked this track!");
      window.location.reload();
    });
  </script>
</body>
</html>